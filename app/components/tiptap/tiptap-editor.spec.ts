import { describe, expect, it } from 'vitest'
import TiptapEditor from './TiptapEditor.vue'

describe('TiptapEditor', () => {
  it('exports the rich text editor component', () => {
    expect(TiptapEditor).toBeDefined()
  })
})

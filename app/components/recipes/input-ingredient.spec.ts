import { describe, expect, it } from 'vitest'
import InputIngredient from './InputIngredient.vue'

describe('InputIngredient', () => {
  it('exports the ingredient editor component', () => {
    expect(InputIngredient).toBeDefined()
  })
})

import { describe, expect, it } from 'vitest'
import RecipesGrid from './RecipesGrid.vue'

describe('RecipesGrid', () => {
  it('exports the recipe grid component', () => {
    expect(RecipesGrid).toBeDefined()
  })
})

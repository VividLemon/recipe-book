import { describe, expect, it } from 'vitest'
import ShowRecipeModal from './ShowRecipeModal.vue'

describe('ShowRecipeModal', () => {
  it('exports the recipe modal component', () => {
    expect(ShowRecipeModal).toBeDefined()
  })
})

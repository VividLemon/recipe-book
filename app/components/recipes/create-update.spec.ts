import { describe, expect, it } from 'vitest'
import CreateUpdate from './CreateUpdate.vue'

describe('CreateUpdate', () => {
  it('exports the recipe form component', () => {
    expect(CreateUpdate).toBeDefined()
  })
})

import { describe, expect, it } from 'vitest'
import TagCreateModal from './TagCreateModal.vue'

describe('TagCreateModal', () => {
  it('exports the tag creation modal component', () => {
    expect(TagCreateModal).toBeDefined()
  })
})

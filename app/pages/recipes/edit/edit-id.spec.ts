import { describe, expect, it } from 'vitest'
import EditRecipePage from './[id].vue'

describe('edit recipe page', () => {
  it('exports the edit recipe page', () => {
    expect(EditRecipePage).toBeDefined()
  })
})

import { describe, expect, it } from 'vitest'
import CreateRecipePage from './create.vue'

describe('create recipe page', () => {
  it('exports the create recipe page', () => {
    expect(CreateRecipePage).toBeDefined()
  })
})

/** Server-facing recipe domain contract. The shared contract remains canonical for the web client. */
export type {
  IngredientData,
  PhotosData,
  RecipeData,
  RecipeDifficultyData
} from '../../types/recipe'
